# local package
-e .

# core data + ML
click
python-dotenv>=0.5.1
pandas
numpy
scikit-learn

# model + training (used by the saved DistilBERT classifier)
torch
transformers
datasets
evaluate
accelerate

# experiment tracking
dagshub
mlflow

# web API + UI
fastapi
uvicorn[standard]
jinja2
python-multipart
pydantic

# docs + tooling
Sphinx
coverage
awscli
flake8
